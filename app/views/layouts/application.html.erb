<!DOCTYPE html>
<html class="h-full bg-gray-100">
  <head>
    <title><%= content_for?(:title) ? yield(:title) : 'Bienvenue' %> - Practice Room</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="description" content="<%= content_for(:description) ? yield(:description) : '' %>">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="h-full relative" data-controller="application popup">
      <%= render partial: "shared/narrow_sidebar" %>

      <main class="md:pl-20 flex w-full items-stretch gap-x-0">
        <div class="flex-1 relative">
          <%= render partial: "shared/header" %>
          <%= yield %>
        </div>

        <!-- Secondary column (hidden on smaller screens) -->
        <% if current_user %>
          <aside class="z-[110] h-screen hidden fixed right-0 bottom-0 w-96 shrink-0 xl:static xl:block"
                 data-application-target="sidebar"
                 data-transition-enter="transform transition ease-in-out duration-500"
                 data-transition-enter-start="translate-x-full"
                 data-transition-enter-end="translate-x-0"
                 data-transition-leave="transform transition ease-in-out duration-500"
                 data-transition-leave-start="translate-x-0"
                 data-transition-leave-end="translate-x-full">
            <div class="fixed h-full right-0 bottom-0 bg-light shadow px-4 overflow-hidden">
              <%= render partial: "shared/sidebar/header" %>
              <div class="flex flex-col h-full gap-6">
                <%= render partial: "shared/sidebar/content" %>
              </div>
            </div>
          </aside>
        <% end %>
      </main>

      <% if current_user %>
        <div data-action="click->popup#clickOutside">
          <dialog data-popup-target="dialog" class="backdrop:bg-gray-500 backdrop:opacity-75 w-full md:w-1/2 rounded-xl">
            <div  class="rounded-lg p-6">
              <%= turbo_frame_tag :exercise_edit %>
            </div>
          </dialog>
          <%= link_to new_exercise_path, class: "hidden rounded-full shadow-lg text-white bg-primary p-3 fixed bottom-10 right-10 md:block z-[500]", title: "Créer un nouvel exercice", data: {turbo_frame: :exercise_edit, action: "click->popup#open"} do %>
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-8 h-8">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
            </svg>
          <% end %>
        </div>
      <% end %>

    <%#
      data-transition-enter="ease-in-out duration-500"
      data-transition-enter-start="opacity-0"
      data-transition-enter-end="opacity-100"
      data-transition-leave="ease-in-out duration-500"
      data-transition-leave-start="opacity-100"
      data-transition-leave-end="opacity-0" %>
    <div data-application-target="overlay"
         class="hidden inset-0 bg-gray-500 bg-opacity-75 transition-opacity fixed left-0 top-0 w-full h-screen z-[100]"></div>
  </body>
</html>
