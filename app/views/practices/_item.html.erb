<li class="px-6 pb-6 border-b">
  <div class="flex space-x-3">
    <div class="flex justify-center items-center w-10 h-10 bg-gray-400 rounded-full ring-8 ring-white">
      <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="white" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
      </svg>
    </div>
    <div class="flex-1 self-center">
      <p>
        Vous avez débuté une session de pratique à <span class="whitespace-nowrap">
        <time class="session__time"><%= l session.created_at, format: '%H:%M:%S' %></time></span>
      </p>
      <p class="text-xs font-medium text-gray-500"><%= session.practices_exercises.count %> exercices</p>
      <ul class="mt-3">
          <% session.practices_exercises.each_with_index do |practiced, index| %>
            <%= turbo_frame_tag "practices_exercises_#{practiced.id}" do %>
              <li class="flex p-3 mb-1 space-x-3 text-white bg-gray-500 rounded"
                data-practices-target="exercise" data-duration="<%= practiced.duration %>">
                <div class="flex-1">
                  <p class="text-sm">
                    <%= link_to practiced.exercise.title, exercise_path(practiced.exercise), data: {turbo: false} %>
                  </p>
                  <p class="text-xs text-gray-300">
                    <span>
                      <%= Time.at(practiced.duration).utc.strftime("%H:%M:%S") %>
                    </span>
                    <span>
                      à <%= Time.at(practiced.created_at).utc.strftime("%H:%M:%S") %>
                    </span>
                  </p>
                  <ul class="flex space-x-3 text-xs">
                    <% practiced.exercise.categories.each do |category| %>
                      <li>
                        <a href="<%= by_slug_categories_path category.slug %>"
                            title="<%= category.description %>" class="font-bold">
                          #<%= category.name %>
                        </a>
                      </li>
                    <% end %>
                  </ul>
                </div>
                <%= link_to remove_from_practice_path(
                  practices_exercises_id: practiced.id),
                  method: :delete, class: "btn btn-secondary self-center text-white hover:text-gray-500" do %>
                  <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                  </svg>
                <% end %>
              </li>
            <% end %>
          <% end %>
        </ul>

    </div>
  </div>



</li>
