<div class="text-center border-b pb-3">
  <h3 class="text-2xl font-medium">Journal de pratique</h3>
  <p class="text-sm font-light text-gray-500">Retrouvez ici tout l'historique de votre pratique.</p>
  <%= button_to "Dernières sessions", get_day_path(current_user.practices.last.created_at), method: :get, class: "text-xs" %>
  <%= button_to "Aujourd'hui", get_day_path(Date.today), method: :get, class: "text-xs" %>
</div>
<div  class="flex-1 flex flex-col justify-between overflow-hidden">
  <%= render partial: "practices/selector", locals: {current_week: Date.today.beginning_of_week..Date.today.end_of_week, practices_for_the_week: current_user.practices_of_the_week } %>
  <div class="flex-1 flex flex-col justify-between overflow-hidden overflow-y-scroll">
    <%= render partial: "practices/list", locals: {practices_of_the_day: current_user.practices_of_the_day} %>
  </div>
</div>
