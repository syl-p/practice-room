
<%= turbo_frame_tag "exercise_versions" do %>
    <div class="panel">
        <div class="panel-header">
            <a href="<%= list_versions_path(id: @exercise.original) %>" class="float-right">
                <i class="mdi mdi-close"></i>
            </a>
            <h3>Nouvelle version:</h3>
            <small>Proposer une version personnalisée de l'exercice: <%= @exercise.original.title %></small>
        </div>
        <div class="panel-body">
            <%= render 'exercises/versions/form', version: @exercise %>
        </div>

        <div class="panel-footer">
            <%=  link_to 'Retour à la liste des versions', list_versions_path(id: @exercise.original) %>
        </div>
    </div>
<% end %>
