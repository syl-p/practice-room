<% size = nil if local_assigns[:size].nil?
class_size = case size
            when :small
              "avatar-sm"
            when :medium
              "avatar-md"
            when :large
              "avatar-lg"
            when :xlarge
              "avatar-xl"
            else
              ""
            end
%>
<figure class="avatar <%= class_size %>"
  data-initial="xx" style="background-color: #5755d9;">
  <% if user.avatar.present? %>
    <img src="<%= user.avatar.url %>" alt="<%= user.username %>">
  <% end %>
</figure>
